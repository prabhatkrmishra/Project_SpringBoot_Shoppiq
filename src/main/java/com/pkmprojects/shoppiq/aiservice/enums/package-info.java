/**
 * Enumerations for AI chat domain values.
 *
 * <p>Contains the enum types that define valid states and roles
 * within the AI chat domain. These include conversation lifecycle
 * statuses ({@code ACTIVE}, {@code RESOLVED}) and message roles
 * ({@code USER}, {@code ASSISTANT}, {@code SYSTEM}, {@code TOOL}).</p>
 *
 * @author prabhatkrmishra
 * @since 1.0.0
 */
package com.pkmprojects.shoppiq.aiservice.enums;

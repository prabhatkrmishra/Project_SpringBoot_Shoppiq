/**
 * Public-facing REST controllers.
 *
 * <p>Handles HTTP requests for storefront pages, shared controller advice,
 * and delegates business operations to the service layer.</p>
 */
package com.pkmprojects.shoppiq.controller;
